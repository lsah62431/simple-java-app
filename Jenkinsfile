pipeline {
    agent any

   

    stages {
        stage('build') {
            steps {
              echo "test in build"
            }
        }

        stage('test') {
            steps {
                echo "test in progress"
            }
        }

        stage('deploy') {
            steps {
                echo "deploy in progress"
            }
        }
    }



}
